<Application x:Class="MyJournalApp.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             StartupUri="MainWindow.xaml">
    <Application.Resources>
        <ResourceDictionary>
            <!-- Global Font Family -->
            <FontFamily x:Key="PrimaryFont">Segoe UI</FontFamily>
            
            <!-- Color Palette - Dark Theme -->
            <Color x:Key="BackgroundPrimary">#0D1117</Color>
            <Color x:Key="BackgroundSecondary">#161B22</Color>
            <Color x:Key="BackgroundTertiary">#21262D</Color>
            <Color x:Key="BorderColor">#30363D</Color>
            <Color x:Key="TextPrimary">#C9D1D9</Color>
            <Color x:Key="TextSecondary">#8B949E</Color>
            <Color x:Key="TextMuted">#484F58</Color>
            <Color x:Key="AccentBlue">#58A6FF</Color>
            <Color x:Key="AccentGreen">#238636</Color>
            <Color x:Key="AccentGreenHover">#2EA043</Color>
            <Color x:Key="AccentRed">#F85149</Color>
            <Color x:Key="AccentOrange">#F97316</Color>
            <Color x:Key="AccentYellow">#F0B429</Color>
            
            <!-- Solid Color Brushes -->
            <SolidColorBrush x:Key="BackgroundPrimaryBrush" Color="{StaticResource BackgroundPrimary}"/>
            <SolidColorBrush x:Key="BackgroundSecondaryBrush" Color="{StaticResource BackgroundSecondary}"/>
            <SolidColorBrush x:Key="BackgroundTertiaryBrush" Color="{StaticResource BackgroundTertiary}"/>
            <SolidColorBrush x:Key="BorderBrush" Color="{StaticResource BorderColor}"/>
            <SolidColorBrush x:Key="TextPrimaryBrush" Color="{StaticResource TextPrimary}"/>
            <SolidColorBrush x:Key="TextSecondaryBrush" Color="{StaticResource TextSecondary}"/>
            <SolidColorBrush x:Key="TextMutedBrush" Color="{StaticResource TextMuted}"/>
            <SolidColorBrush x:Key="AccentBlueBrush" Color="{StaticResource AccentBlue}"/>
            <SolidColorBrush x:Key="AccentGreenBrush" Color="{StaticResource AccentGreen}"/>
            
            <!-- Global TextBlock Style -->
            <Style TargetType="TextBlock">
                <Setter Property="FontFamily" Value="{StaticResource PrimaryFont}"/>
            </Style>
            
            <!-- Global TextBox Style -->
            <Style TargetType="TextBox">
                <Setter Property="FontFamily" Value="{StaticResource PrimaryFont}"/>
                <Setter Property="Background" Value="{StaticResource BackgroundSecondaryBrush}"/>
                <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}"/>
                <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}"/>
                <Setter Property="BorderThickness" Value="1"/>
                <Setter Property="Padding" Value="12,10"/>
                <Setter Property="FontSize" Value="14"/>
            </Style>
            
            <!-- Global Button Style -->
            <Style TargetType="Button">
                <Setter Property="FontFamily" Value="{StaticResource PrimaryFont}"/>
                <Setter Property="Cursor" Value="Hand"/>
            </Style>
            
            <!-- ScrollBar Style for Dark Theme -->
            <Style TargetType="ScrollBar">
                <Setter Property="Background" Value="Transparent"/>
                <Setter Property="Width" Value="8"/>
            </Style>
        </ResourceDictionary>
    </Application.Resources>
</Application>
